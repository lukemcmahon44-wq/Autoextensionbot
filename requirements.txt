# Pinned to the versions this bot was developed and tested against.
# (cryptography & PyYAML are pinned to the platform-provided builds to avoid
#  clobbering distro packages; any cryptography >= 41 supports RSA-PSS signing.)
requests==2.32.3
cryptography==41.0.7
PyYAML==6.0.1
tenacity==9.0.0
python-dotenv==1.0.1

# Dev / test only
pytest==8.3.3
